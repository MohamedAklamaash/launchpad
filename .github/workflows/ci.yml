name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - "**"

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check-onboarding-scripts:
    # Guards against IAM policy drift between create_aws_role.sh and
    # update_aws_role.sh — a real bug last hit was codebuild:* being dropped
    # from the update script, which broke deployments for any customer who ran
    # the refresh. This is a stdlib-only check so it stays fast.
    name: Onboarding script policy sync
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install pytest
        run: pip install pytest

      - name: Verify LaunchpadDeploymentPolicy is in sync
        run: python3 app_scripts/_check_policy_sync.py

      - name: Run policy-sync guard tests
        run: pytest app_scripts/test_check_policy_sync.py -v

  check-python:
    name: Lint & Syntax (python services)
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service:
          - gateway-service
          - payment-service
          - deployment-services
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install ruff
        run: pip install ruff

      - name: Syntax check
        run: python -m compileall ${{ matrix.service }} -q

      - name: Ruff lint
        run: ruff check ${{ matrix.service }}

  check-identity:
    name: Lint & Typecheck (identity-services)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: identity-services
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          package_json_file: identity-services/package.json

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: identity-services/pnpm-lock.yaml

      - run: pnpm install --frozen-lockfile

      - name: Build common
        run: pnpm --filter @launchpad/common build

      - name: Prettier
        run: pnpm format

      - name: ESLint
        run: pnpm lint

      - name: TypeScript
        run: pnpm -r --workspace-root=false exec tsc --noEmit

  check-frontend:
    name: Lint & Typecheck (frontend)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: launchpad-frontend
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: launchpad-frontend/pnpm-lock.yaml

      - run: pnpm install --frozen-lockfile

      - name: ESLint
        run: pnpm lint

      - name: TypeScript
        run: npx tsc --noEmit
